openapi: 3.0.1
info:
  title: ActionGPT Gmail Server
  version: "3.0"
  description: >
    ActionGPT Gmail API — 24/7 backend for Gmail automation.
    Supports reading, sending, deleting, archiving, managing labels,
    threads, quota, and status.

servers:
  - url: https://actiongpt-server.ngrok.app

paths:
  /health:
    get:
      summary: Health Check
      operationId: health_check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

---
permalink: /actiongpt.yaml
layout: null
---
openapi: 3.0.1
info:
  title: ActionGPT Gmail Server
  version: "3.0"
  description: >
    Minimal OpenAPI schema for ChatGPT validation test.
    Backend health check endpoint.

servers:
  - url: https://actiongpt-server.ngrok.app

paths:
  /health:
    get:
      summary: Health Check
      operationId: health_check
      responses:
        "200":
          description: Server is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
